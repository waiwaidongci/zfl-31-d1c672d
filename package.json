{
  "name": "zfl-31",
  "version": "1.0.0",
  "description": "手工织锦纹样排版台 - 纯静态前端应用",
  "private": true,
  "scripts": {
    "test": "mocha --require tests/test-setup.js 'tests/**/*.test.js' --reporter spec",
    "check:html": "node scripts/check-html-refs.js",
    "check:order": "node scripts/check-module-order.js",
    "check": "npm run check:html && npm run check:order && npm run test",
    "ci": "npm run check"
  },
  "devDependencies": {
    "mocha": "^10.4.0",
    "chai": "^4.4.1",
    "jsdom": "^24.0.0"
  }
}
